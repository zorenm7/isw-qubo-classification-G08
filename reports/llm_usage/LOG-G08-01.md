## Metadata

```yaml
group_id: "G08"
repository_url: "https://github.com/zorenm7/isw-qubo-classification-G08"
students:
  - matricola: "66480"
    name: "Lorenzo Murgia"
  - matricola: "66463"
    name: "Nicolò Farci"
last_update: "2026-06-25"
log_id: "G08-01"
```

---

# Interaction Entries

---

## Interaction G08-06-23-001

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Strategic Project Planning & Prompt Engineering Setup"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "requirements_understanding"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "Progetto_ISW_2025-26_specifiche.pdf"
      repository_path: null
      description: "Official project document containing constraints and instructions"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
I need to submit my Software Engineering (ISW) project, and the professor explicitly told us to use AI to complete it via prompting. I want you to help me create these prompts following prompt engineering best practices. The interaction with the AI that will receive these prompts must be in English. For any detailed specifications, strictly follow the professor's instructions provided in the attached PDF.
```

### 6. LLM response

```text
This is a very interesting and detailed project. Given your usual use of Python in data science and machine learning, the parts related to training classifiers with libraries like scikit-learn and feature extraction will certainly be familiar to you, and you will be able to easily verify their correctness. We can therefore exploit prompt engineering to ensure that the LLM faithfully respects the interfaces and the QUBO optimization logic, which is the most mathematically complex part.

To get the best code, we will divide the project into logical modules. Asking for everything in a single prompt would lead to poor quality code full of errors.

Here is a prompting strategy structured according to best practices (Role-Task-Context-Constraints), ready to be copied and pasted.

Prompt 1: Project Setup & Preprocessing
This prompt sets the general context for the LLM and addresses Phase 1, ensuring that the requirements for the functions and output files are met.
Copy and paste to the LLM:
Role: Act as a Senior Python Software Engineer and Data Scientist.
Context: I am building a binary classification software engineering project using Python 3.11+. The project requires reading a dataset, reducing features using a QUBO optimization algorithm, training classifiers, and predicting results. I need you to help me write the code module by module.
Task: Write the code for src/qubo_project/preprocessing.py.
Constraints & Specifications:
You must implement exactly this function signature: def fit_normalize(input_csv: str, target_column: str, normalized_csv: str, outInitalRes_json: str, minPercValid: float = 0.05):.
Logic: Read the CSV (the target column name is a variable, do not hardcode it). Drop features (columns) that contain a percentage of missing or zero values greater than minPercValid.
Apply z-score standardization to normalize all remaining features, excluding the target column.
Save the normalized dataset to normalized_csv (keeping the original column headers and target column).
Save execution statistics to a JSON file (outInitalRes_json) containing: n_input_features, n_kept_features, dataset_size, dataset_input_time, dataset_processing_time, and dropped_feature_names.
The script must also be executable via CLI with argparse handling the arguments: --input, --target, --out-data, --out-json, --min-perc-valid.
Do not use absolute paths. Ensure code is modular and clean.

Prompt 2: The QUBO Feature Selection (Core Logic)
This is the heart of the project. We give the LLM the exact formula and tell it to use an iterative approach to find the alpha value.
Copy and paste to the LLM:
Role: Act as a Quantum Optimization and Data Science Expert.
Task: Write the code for src/qubo_project/feature_selection.py.
Constraints & Specifications:
Implement exactly this function signature: def select_features(normalized_csv: str, reducedTrain_csv: str, reducedTest_csv: str, output_ottim_csv: str, output_json: str, target_column: str, percTest: float = 0.30, allowance: int = 1, seed: int = 42, percSelected: float = 0.20, alpha_computations: int = 100):.
Math & QUBO formulation: The objective function to minimize is $f(x)=-[\alpha\sum_{j=1}^{n}x_{j}|\rho_{Vj}|-(1-\alpha)\sum_{j=1}^{n}\sum_{k=1}^{n}x_{j}x_{k}|\rho_{jk}|]$, where rho is the Spearman correlation. Build the dense Q matrix based on this.
Optimization Loop: You must not hardcode the number of features. Instead, write a loop that varies the alpha parameter (between 0 and 1) up to alpha_computations times. The goal is to find an alpha that selects a number of features equal to percSelected of the total features, +/- the allowance.
Once the optimal binary vector is found, drop the unselected features from the dataset, split the dataset into a training set and a test set using a clean cut at sample M (calculated from percTest), and save them.
Save optimization logs to output_ottim_csv and final stats to output_json.
Implement the CLI arguments: --in-normalized, --out-train, --out-test, --out-optimizations, --out-json, --target, --perc-selected, --allowance, --perc-test, --seed, --alpha-computations.

Prompt 3: Model Training & Testing
This prompt covers Phase 3 and Phase 4, focusing on training and output metrics.
Copy and paste to the LLM:
Role: Act as a Machine Learning Engineer.
Task: Write the code for src/qubo_project/model.py.
Constraints & Specifications:
Implement three binary classification algorithms using scikit-learn. One MUST be Random Forest.
Implement this training function: def train(classifier: str, reducedTrain_csv: str, target_column: str, model_path: str, metrics_json: str, seed: int = 42):. Save the trained model in .joblib format.
Implement this prediction function: def predict(reduced_Test_csv: str, target_column: str, model_path: str, predictions_csv: str, classif_stats_json: str):.
Outputs: The prediction CSV must contain columns: row_n, target, prediction, score. The classification stats JSON must calculate and include: Total samples, target=1 percentage, Precision/Recall/F1 for both class 0 and class 1, ROC-AUC, and the Confusion Matrix.
Implement CLI interfaces for both train and predict modes with the appropriate arguments.

Prompt 4: Pytest & GUI
This prompt covers the auxiliary tools required for the final evaluation.
Copy and paste to the LLM:
Task: Now, generate the automated tests and the GUI for this project.
Constraints & Specifications:
Tests: Write a test suite using pytest inside the tests/ directory. Create tests that verify: preprocessing handles missing values and produces numerical columns, feature selection outputs a binary vector and targets ~20% of features, training produces a saved model, and predicting outputs a correctly formatted CSV.
GUI: Create a simple interface using streamlit in src/qubo_project/gui.py. It should allow a user to select a dataset, run preprocessing, run feature selection, train the model, execute predictions, and view the outputs. Include basic error handling for invalid inputs.

Once the LLM generates these codes for you, remember to paste the logs of your conversations into the markdown files llm_usage/LOG-GXX-01.md using the template prescribed by the professor.

Do you want us to analyze in detail how to manage the GitHub repository structure and the requirements.txt file before starting to have the AI generate the code?
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The strategy proved fundamental in guiding the subsequent code generation process efficiently."
```

## Interaction G08-06-23-002

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Python Data Preprocessing Module Development"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "Progetto_ISW_2025-26_specifiche.pdf"
      repository_path: null
      description: "Official project document containing constraints and instructions"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
Role: Act as a Senior Python Software Engineer and Data Scientist.
Context: I am building a binary classification software engineering project using Python 3.11+. The project requires reading a dataset, reducing features using a QUBO optimization algorithm, training classifiers, and predicting results. I need you to help me write the code module by module.
Task: Write the code for src/qubo_project/preprocessing.py.
Constraints & Specifications:
You must implement exactly this function signature: def fit_normalize(input_csv: str, target_column: str, normalized_csv: str, outInitalRes_json: str, minPercValid: float = 0.05):.
Logic: Read the CSV (the target column name is a variable, do not hardcode it). Drop features (columns) that contain a percentage of missing or zero values greater than minPercValid.
Apply z-score standardization to normalize all remaining features, excluding the target column.
Save the normalized dataset to normalized_csv (keeping the original column headers and target column).
Save execution statistics to a JSON file (outInitalRes_json) containing: n_input_features, n_kept_features, dataset_size, dataset_input_time, dataset_processing_time, and dropped_feature_names.
The script must also be executable via CLI with argparse handling the arguments: --input, --target, --out-data, --out-json, --min-perc-valid.

Do not use absolute paths. Ensure code is modular and clean.
```

### 6. LLM response

```yaml
response_stored_in_file:
  used: true
  file_name: "LLM_G08-06-23-002_response.md"
  repository_path: "reports/llm_usage/files/LLM_G08-06-23-002_response.md"
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G08-06-23-002_preprocessing.py"
    repository_path: "reports/llm_usage/files/LLM_G08-06-23-002_preprocessing.py"
    file_type: "py"       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: "Step 1: Data preprocessing and normalization module for the binary classification project."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - "src/qubo_project/preprocessing.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The generated code strictly adhered to the requested constraints, successfully integrating the scikit-learn StandardScaler and generating the exact JSON schema required without any need for manual debugging."
```

---

## Interaction G08-06-23-003

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Strategic Project Planning & Prompt Engineering Setup"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "requirements_understanding"
  - "preprocessing"
```

### 4. Input files and/or context provided to the LLM


```yaml
input_context:
  files_uploaded:
    - file_name: "LLM_G08-06-23-002_preprocessing.py"
      repository_path: "reports/llm_usage/files/"
      description: "Step 1: Data preprocessing and normalization module for the binary classification project."
  code_snippets: "yes"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```


### 5. Student prompt

```text
Please review the generated code. If it is correct and meets all specifications, provide the next prompt to generate the code for src/qubo_project/feature_selection.py.
```

### 6. LLM response

```text
The generated code is excellent and follows the project specifications almost exactly.

Analysis of the Generated Code

* Function Signature: Perfect. The typed parameters match the professor’s requirements exactly.
* Filtering Logic: The use of vectorized pandas boolean masks (df.notna() & (df != 0)) is both efficient and idiomatic. It avoids slow loops, which is especially important given that the final test dataset will contain more than 1.5 million samples.
* Normalization: Using StandardScaler from scikit-learn is the standard and most appropriate approach for applying the required z-score standardization.
* Output Management: The implementation correctly generates the JSON file with all the required keys and saves the CSV while preserving the original headers.
* CLI Interface: argparse has been implemented using the exact command-line flags specified in the project requirements.

One small recommendation for the future development of the repository: make sure to record the exact versions of pandas and scikit-learn used in your virtual environment so they can later be included in the final requirements.txt file.

⸻

We can now move on to the core algorithmic component of the project.

Prompt 2: QUBO-Based Feature Selection

Copy and paste the following prompt into the LLM. I have structured it to ensure the AI clearly understands both the iterative search for the parameter $\alpha$ and the mathematical requirements of the objective function. I have also explicitly suggested using well-established libraries such as dimod or neal (from D-Wave) to solve the QUBO problem using Simulated Annealing.

Role: Act as a Quantum Optimization and Data Science Expert.

Task: Write the code for src/qubo_project/feature_selection.py.

Constraints & Specifications:

* Function Signature: You must exactly implement:
    def select_features(normalized_csv: str, reducedTrain_csv: str, reducedTest_csv: str, output_ottim_csv: str, output_json: str, target_column: str, percTest: float = 0.30, allowance: int = 1, seed: int = 42, percSelected: float = 0.20, alpha_computations: int = 100):
* Math & QUBO Formulation: The objective function to minimize is
    $f(x)=-[\alpha\sum_{j=1}^{n}x_{j}|\rho_{Vj}|-(1-\alpha)\sum_{j=1}^{n}\sum_{k=1}^{n}x_{j}x_{k}|\rho_{jk}|]$,
    where $\rho$ denotes the Spearman correlation. Construct the dense Q matrix according to this formulation. The terms $\alpha x_{j}|\rho_{Vj}|$ should be placed on the diagonal, while the terms $-(1-\alpha)|\rho_{jk}|$ should populate the off-diagonal entries.
* Optimization Loop: Do not hardcode the final number of selected features ($K$). Instead, implement a loop that varies the parameter $\alpha$ between 0 and 1 for up to alpha_computations iterations. The objective is to identify an $\alpha$ that produces an optimal binary solution vector containing a number of selected features equal to percSelected of the total input features, within a tolerance of ± allowance.
* Dataset Splitting: Once the optimal binary vector has been found, remove all unselected features from the original dataset. Then split the resulting dataset into training and test sets using a deterministic split at sample $M$, where $M$ is computed according to percTest. Save both datasets while preserving the original column headers and the target column.
* Outputs: Save the optimization log for every tested value of $\alpha$ to output_ottim_csv.csv with the following columns:
    * alpha
    * optimization_time
    * number_of_features
    * cost_function_value
    Save the final execution statistics to output_json using the following schema:
    * n_features
    * target_ratio
    * target_k
    * allowance
    * n_selected
    * alpha
    * selected_vector
    * selected_feature_names
    * algorithm
    * seed
    * alpha_computations
    * percTest
    * training_dataset_size
    * test_dataset_size
    * q_matrix_creation_time
    * mean_optimization_time
    * std_dev_optimization_time
* CLI Interface: Implement an argparse interface supporting the following command-line arguments:
    * --in-normalized
    * --out-train
    * --out-test
    * --out-optimizations
    * --out-json
    * --target
    * --perc-selected
    * --allowance
    * --perc-test
    * --seed
    * --alpha-computations
* Libraries: Use a well-established open-source library such as dimod together with neal (Simulated Annealing) to solve the QUBO optimization problem. Clearly document your choice in the source code comments.

Would you like us to review the code generated for this module together before moving on to Prompt 3, paying particular attention to how the Q matrix is constructed and how the Spearman correlation is computed?
```

### 7. Files generated or downloaded from the LLM response


```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false  # true / false
  modified_before_use: true   # true / false
  description_of_modifications: "We removed the conversational intro/outro generated by the LLM"
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: null    # "yes"/"no"  The subsequent part only if "yes"
  issue_categories:    # Delete non-pertinent ones
   - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: null
```
## Metadata

```yaml
group_id: "G08"
repository_url: "https://github.com/zorenm7/isw-qubo-classification-G08"
students:
  - matricola: "66480"
    name: "Lorenzo Murgia"
  - matricola: "66463"
    name: "Nicolò Farci"
last_update: "2026-06-26"
log_id: "G08-05"
```

---

# Interaction Entries

---

## Interaction G08-06-24-006

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Python Preprocessing Module Development"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "testing"
  - "gui"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null
```

### 5. Student prompt

```text
Role: Act as a QA Automation Engineer and Frontend Python Developer.
Task: Write the automated test suite and the graphical user interface for our QUBO Feature Selection project.

Part 1: Automated Tests (tests/test_pipeline.py) Constraints & Specifications:
- Use pytest.
- The tests must assume the existence of a small mock dataset located at data/sample_test_dataset.csv.
- Write specific test functions to verify the following exact requirements:
  1. Preprocessing produces only numerical columns.
  2. Preprocessing correctly handles missing/null values (drops columns exceeding the threshold).
  3. Normalization produces a valid dataset (e.g., checks if values are scaled).
  4. Feature selection produces a valid binary vector (only 0s and 1s).
  5. The number of selected features is approximately the requested percentage (≈20%).
  6. Training successfully produces and saves a .joblib model to disk.
  7. Prediction successfully produces a CSV with the required columns (row_n, target, prediction, score).
- Ensure tests create temporary directories/files for outputs to avoid polluting the main project directory, using pytest's tmp_path fixture.

Part 2: Graphical User Interface (src/qubo_project/gui.py) Constraints & Specifications:
- Use streamlit to build the GUI.
- The interface must allow a human user to logically step through the pipeline:
  1. Select/Upload an input dataset (CSV).
  2. Execute the preprocessing phase (with a slider or number input for minPercValid).
  3. Execute the QUBO feature selection (with inputs for percSelected, allowance, alpha_computations).
  4. Execute model training (dropdown to select random_forest, logistic_regression, or gradient_boosting).
  5. Execute predictions on the test set.
  6. Visualize the main outputs (e.g., display the classification stats JSON, the ROC-AUC, and the selected features).
- Include basic error handling (e.g., displaying an error message if the user tries to train a model before running feature selection).
- Ensure the GUI imports and uses the functions from preprocessing.py, feature_selection.py, and model.py rather than duplicating their logic.
```

### 6. LLM response

```yaml
response_stored_in_file:
  used: true
  file_name: "LLM_G08-06-24-006_response.md"
  repository_path: "reports/llm_usage/files/LLM_G08-06-24-006_response.md"
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G08-06-24-006_test_pipeline.py"
    repository_path: "reports/llm_usage/files/LLM_G08-06-24-006_test_pipeline.py"
    file_type: "py"
    created_from_response: true
    short_description: "Automated pytest suite for the classification pipeline."
    referenced_in_response: true
  - unique_file_name: "LLM_G08-06-24-006_gui.py"
    repository_path: "reports/llm_usage/files/LLM_G08-06-24-006_gui.py"
    file_type: "py"
    created_from_response: true
    short_description: "Streamlit graphical user interface for the pipeline."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: false
  modified_before_use: true
  description_of_modifications: "Updated module import paths in both the test suite and GUI from 'qubo_project' to 'src.qubo_project' to match the repository structure requirements."
  related_repository_files:
    - "tests/test_pipeline.py"
    - "src/qubo_project/gui.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"
  issue_categories:
    - "other"
  description: "The AI assumed the python path would resolve 'qubo_project' directly, ignoring the 'src/' parent folder constraint during the import statements."
  how_issue_was_resolved: "Manually prepended 'src.' to the absolute import statements in both files to ensure correct module resolution."
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 4
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The test coverage using the tmp_path fixture was excellent. The Streamlit session state logic perfectly fulfilled the sequential pipeline requirement. The minor import issue was quickly addressed during code review."
```

---

## Interaction G08-06-24-007

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "Strategic Project Planning & Prompt Engineering Setup"
interaction_mode: "web_chat"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

- "code_review"
- "repository_structure"

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "LLM_G08-06-24-006_test_pipeline.py"
      repository_path: "reports/llm_usage/files/"
      description: "Generated test suite for review."
    - file_name: "LLM_G08-06-24-006_gui.py"
      repository_path: "reports/llm_usage/files/"
      description: "Generated Streamlit GUI for review."
  code_snippets: "yes"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
This was the response to the previous prompt (Part 1 and Part 2). Please tell me what you think of the code:
```

### 6. LLM response

```text
The generated code is excellent and closes the loop in a very elegant way. Presenting a test suite that leverages `tmp_path` (ensuring the autograder or environment isn't polluted with temporary files) and a state-managed Streamlit GUI (`st.session_state` to prevent out-of-order executions) will certainly make a great impression during the evaluation of a university project.

Just pay attention to a "sneaky" technical detail when assembling everything: in the test file, the AI wrote `from qubo_project.preprocessing import...`. Since the specifications dictate that the folder must be inside `src/`, you might need to change the import to `from src.qubo_project.preprocessing import...` or remember to run `pytest` from the project root while correctly configuring the `PYTHONPATH`. It's a classic configuration hurdle, but easily fixed.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"
  issue_categories:
    - "none"
  description: null
  how_issue_was_resolved: null
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The AI accurately identified the subtle Python import path issue and generated an optimal final prompt to establish the repository documentation and the detailed requirements file."
```